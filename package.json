{
  "name": "healthcare-appointment-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack Healthcare Appointment & Follow-up Manager with AI triage, concurrency control, and Google Calendar sync",
  "scripts": {
    "install:all": "npm --prefix server install --include=dev && npm --prefix client install --include=dev",
    "server": "node server/src/server.js",
    "start": "node server/src/server.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "db:setup": "npm --prefix server run db:setup",
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
